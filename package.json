{
	"name": "hivemind",
	"version": "0.0.0",
	"private": true,
	"description": "Hivemind — a multi-agent operating system that runs inside Notion. Notion is the hive, your agents are the swarm.",
	"scripts": {
		"build": "tsc",
		"check": "tsc --noEmit",
		"deploy": "ntn workers deploy",
		"env:push": "ntn workers env push"
	},
	"engines": {
		"node": ">=22.0.0",
		"npm": ">=10.9.2"
	},
	"devDependencies": {
		"@types/node": "^22.9.0",
		"tsx": "^4.20.6",
		"typescript": "^5.8.0"
	},
	"dependencies": {
		"@anthropic-ai/sdk": "^0.96.0",
		"@notionhq/workers": ">=0.0.0",
		"openai": "^6.38.0",
		"playwright": "^1.60.0",
		"pusher": "^5.3.3"
	}
}
